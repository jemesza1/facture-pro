function setStatus(id,status){const inv=state.invoices.find(i=>i.id===id);if(inv&&STATUS[status]){const was=inv.status;inv.status=status;saveData();if(status==='payee'&&was!=='payee'){try{fireConfetti();}catch(e){}toast(t('toast.invoicePaid'));}else toast(t('toast.statusUpdated'));renderPage();}}
function deleteInvoice(id){if(!confirm(t('confirm.deleteInvoice')))return;state.invoices=state.invoices.filter(i=>i.id!==id);saveData();toast(t('toast.invoiceDeleted'));renderPage();}
function duplicateInvoice(id){const inv=state.invoices.find(i=>i.id===id);if(!inv)return;const year=new Date().getFullYear();let n=state.nextInvoiceNumber,number;do{number=`FAC-${year}-${String(n).padStart(3,'0')}`;n++;}while(state.invoices.some(i=>i.number===number));state.nextInvoiceNumber=n-1;state.invoices.push({...JSON.parse(JSON.stringify(inv)),id:uid(),number,status:'brouillon',date:new Date().toISOString().slice(0,10)});state.nextInvoiceNumber++;saveData();toast(t('toast.invoiceDuplicated'));renderPage();}
function renderClients(){return`<div class="flex justify-between mb-5"><p class="text-slate-500 text-sm">${state.clients.length} ${t('clients.count')}</p><div class="flex gap-2">${state.clients.length?`<button onclick="exportExcel('clients')" class="btn-secondary"><i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Excel</button>`:''}<button onclick="openClientModal()" class="btn-primary"><i data-lucide="user-plus" class="w-4 h-4"></i> ${t('actions.newClient')}</button></div></div><div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4">${state.clients.map(c=>`<div class="card p-5"><div class="flex justify-between"><div class="min-w-0"><h3 class="font-semibold truncate">${esc(c.name)}</h3><p class="text-sm text-slate-500 truncate">${esc(c.email)||'—'}</p></div><div class="flex"><button onclick="openClientModal('${c.id}')" class="btn-ghost p-2"><i data-lucide="pencil" class="w-4 h-4"></i></button><button onclick="deleteClient('${c.id}')" class="btn-ghost p-2 text-red-500"><i data-lucide="trash-2" class="w-4 h-4"></i></button></div></div><div class="mt-3 text-xs text-slate-600"><p class="whitespace-pre-line">${esc(c.address)}</p>${c.nif?`<p class="font-medium mt-1">${t('clients.nif')} : ${esc(c.nif)}</p>`:''}</div></div>`).join('')}</div>`;}
function deleteClient(id){const hasInv=state.invoices.some(i=>i.clientId===id);if(hasInv){if(!confirm(t('confirm.deleteClientInvoices')))return;}else{if(!confirm(t('confirm.deleteClient')))return;}state.clients=state.clients.filter(c=>c.id!==id);saveData();toast(t('toast.clientDeleted'));renderPage();}
/* La galerie montre les huit mis en avant, et deroule les vingt et un autres
   sur demande. Rien n'est supprime : voir TEMPLATES_TOP dans a.js pour la
   raison. L'etat vit en memoire et pas dans les donnees enregistrees — c'est
   une facon de regarder la page, pas une preference du commercant. */
var _tplAll=false;
function showAllTemplates(){_tplAll=true;renderPage();}
function tplCard(tp){return `<div class="card p-4 cursor-pointer hover:shadow-md" onclick="previewTemplate('${tp.id}')"><div class="h-3 rounded-full mb-3" style="background:${tp.color2?`linear-gradient(90deg,${tp.color},${tp.color2})`:tp.color}"></div><h3 class="font-semibold">${esc(tp.name)}</h3><p class="text-xs text-slate-500 mt-1">${esc(tp.desc)}</p><button class="btn-secondary mt-3 text-xs w-full"><i data-lucide="eye" class="w-3.5 h-3.5"></i> ${t('actions.preview')}</button></div>`;}
function renderTemplates(){
  var top=TEMPLATES.filter(function(x){return TEMPLATES_TOP.indexOf(x.id)>=0;})
                   .sort(function(a,b){return TEMPLATES_TOP.indexOf(a.id)-TEMPLATES_TOP.indexOf(b.id);});
  var rest=TEMPLATES.filter(function(x){return TEMPLATES_TOP.indexOf(x.id)<0;});
  var shown=_tplAll?top.concat(rest):top;
  var grid='<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">'+shown.map(tplCard).join('')+'</div>';
  var more=_tplAll?'':`<div class="mt-5 text-center"><button type="button" onclick="showAllTemplates()" class="btn-secondary"><i data-lucide="layers" class="w-4 h-4"></i> ${esc(t('tpl.showAll').replace('{n}',String(rest.length)))}</button></div>`;
  return `<div class="mb-5"><h3 class="font-semibold text-lg">${shown.length} ${t('tpl.wordTemplates')}</h3><p class="text-sm text-slate-500">${ltrCodes(t('company.compliance'))}</p></div>`+grid+more;}
