function setStatus(id,status){const inv=state.invoices.find(i=>i.id===id);if(inv&&STATUS[status]){const was=inv.status;inv.status=status;saveData();if(status==='payee'&&was!=='payee'){try{fireConfetti();}catch(e){}toast('Facture payée 🎉');}else toast('Statut mis à jour');renderPage();}}
function deleteInvoice(id){if(!confirm('Supprimer cette facture ?'))return;state.invoices=state.invoices.filter(i=>i.id!==id);saveData();toast('Facture supprimée');renderPage();}
function duplicateInvoice(id){const inv=state.invoices.find(i=>i.id===id);if(!inv)return;const year=new Date().getFullYear();const number=`FAC-${year}-${String(state.nextInvoiceNumber).padStart(3,'0')}`;state.invoices.push({...JSON.parse(JSON.stringify(inv)),id:uid(),number,status:'brouillon',date:new Date().toISOString().slice(0,10)});state.nextInvoiceNumber++;saveData();toast('Facture dupliquée');renderPage();}
function renderClients(){return`<div class="flex justify-between mb-5"><p class="text-slate-500 text-sm">${state.clients.length} client(s)</p><button onclick="openClientModal()" class="btn-primary"><i data-lucide="user-plus" class="w-4 h-4"></i> Nouveau client</button></div><div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4">${state.clients.map(c=>`<div class="card p-5"><div class="flex justify-between"><div class="min-w-0"><h3 class="font-semibold truncate">${c.name}</h3><p class="text-sm text-slate-500 truncate">${c.email||'—'}</p></div><div class="flex"><button onclick="openClientModal('${c.id}')" class="btn-ghost p-2"><i data-lucide="pencil" class="w-4 h-4"></i></button><button onclick="deleteClient('${c.id}')" class="btn-ghost p-2 text-red-500"><i data-lucide="trash-2" class="w-4 h-4"></i></button></div></div><div class="mt-3 text-xs text-slate-600"><p class="whitespace-pre-line">${c.address||''}</p>${c.nif?`<p class="font-medium mt-1">NIF : ${c.nif}</p>`:''}</div></div>`).join('')}</div>`;}
function deleteClient(id){const hasInv=state.invoices.some(i=>i.clientId===id);if(hasInv){if(!confirm('Ce client a des factures.\n\nOK = supprimer le client (les factures restent)\nAnnuler = ne rien faire'))return;}else{if(!confirm('Supprimer ce client ?'))return;}state.clients=state.clients.filter(c=>c.id!==id);saveData();toast('Client supprimé');renderPage();}
function renderTemplates(){return`<div class="mb-5"><h3 class="font-semibold text-lg">${TEMPLATES.length} modèles professionnels</h3><p class="text-sm text-slate-500">Conformité Décret 05-468</p></div><div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">${TEMPLATES.map(t=>`<div class="card p-4 cursor-pointer hover:shadow-md" onclick="previewTemplate('${t.id}')"><div class="h-3 rounded-full mb-3" style="background:${t.color2?`linear-gradient(90deg,${t.color},${t.color2})`:t.color}"></div><h3 class="font-semibold">${t.name}</h3><p class="text-xs text-slate-500 mt-1">${t.desc}</p><button class="btn-secondary mt-3 text-xs w-full"><i data-lucide="eye" class="w-3.5 h-3.5"></i> Aperçu</button></div>`).join('')}</div>`;}
