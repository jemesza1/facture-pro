{
  "name": "facture-pro",
  "version": "2.1.1",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp -f index.html styles.css i18n.js public/ && if [ -f app.js ] && [ $(wc -c < app.js) -gt 1000 ]; then cp -f app.js public/; else curl -fsSL https://facture-pro-iota.vercel.app/app.js -o public/app.js; fi"
  }
}
